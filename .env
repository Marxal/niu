VITE_SUPABASE_URL=https://qrokiaztsbmevrdqaeih.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_XekeoE87W9Gyeu-3drdP_A_YbFb9Ffs

# See .env.example for what this is and the two settings it needs in Google
# Cloud Console. Empty means the calendar does not push to Google.
VITE_GOOGLE_CLIENT_ID=

# The VAPID public key, for phone notifications (round 17). Public by design,
# like the two above: the phone hands it to Google's push service so the service
# can check that a notification was signed by whoever holds the matching private
# key. The *private* half lives only in a Supabase Edge Function secret called
# NIU_VAPID_KEYS and must never appear in this repo.
#
# Leave it empty and the app works exactly as before — the Notifications card in
# Settings says it is not set up instead of offering a switch that cannot work.
VITE_VAPID_PUBLIC_KEY=BD0PF7lqIlevEdJ3BNtPPQO5F-1ISscstgxy58Iq4xKXRlgaUKL54Pm4cAy5gVPIeEbGsa5mE1Q0xWLBUMNKq9o
